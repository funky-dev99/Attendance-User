import 'package:flutter/material.dart';

class QrScan extends StatelessWidget {
  const QrScan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Mark In Records'),
      ),
    );
  }
}
