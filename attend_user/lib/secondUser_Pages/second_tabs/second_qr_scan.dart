import 'package:flutter/material.dart';

class SecondQrScan extends StatelessWidget {
  const SecondQrScan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
